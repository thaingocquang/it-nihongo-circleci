const userAvatarController = require('./avatar/user_avatar.controller')
const postImageController = require('./post/post_image.controller')

module.exports = {
    userAvatarController: userAvatarController,
    postImageController: postImageController,
}
